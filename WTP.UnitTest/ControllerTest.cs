using AutoFixture;
using FluentAssertions;
using System.Threading.Tasks;
using WTP.Api.Controllers;
using WTP.Domain.Entities;
using Xunit;

namespace WTP.UnitTest
{
    public class ControllerTest
    {
        [Fact]
        public async Task ManagerControllerTest()
        {
            //var mockManagerRepository = new MockManagerRepository();
            //var controller = new ManagerController(mockManagerRepository);
            //var result = await controller.GetManager();
            //result.Value.Should().BeEmpty();

            //var fixture = new Fixture();

            //var manager = fixture.Create<Manager>();
        }
    }
}
